# --- Aplica��o ---
spring.application.name=bibliotecadebusca

# --- Servidor ---
server.port=8080

# --- Banco de Dados (Sua configura��o atual) ---
spring.datasource.url=jdbc:postgresql://localhost:5433/postgres?currentSchema=bibliotecadb
spring.datasource.username=postgres
spring.datasource.password=''

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

spring.mvc.pathmatch.matching-strategy=ant_path_matcher
